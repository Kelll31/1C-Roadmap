# 💡 FAQ, советы экспертов и подводные камни 2026 года

[← Ресурсы](05_Resources.md) | [← Начало](README.md)

---

## ❓ Часто задаваемые вопросы

### 🤖 Искусственный интеллект и 1С

**Q: Заменит ли ИИ (Cursor IDE, ChatGPT) программистов 1С?**

A: Нет. ИИ — это инструмент-множитель, а не замена. Он отлично справляется с генерацией шаблонного кода, написанием шагов для автотестов и базовым рефакторингом. Но бизнес-логику (как правильно рассчитать себестоимость с учётом нового НДС 22%, как настроить движения документа по регистрам расчёта) — ИИ за вас не придумает. Разработчик эволюционирует в **архитектора бизнес-процессов**.

---

**Q: Можно ли давать LLM-моделям реальный коммерческий код предприятия?**

A: **Нет!** Использование публичных моделей (ChatGPT, Claude, Gemini) для анализа реального ERP-кода нарушает коммерческую тайну. В 2026 году компании либо разворачивают **локальные модели** (например, через [Ollama](https://ollama.com)), либо используют корпоративные защищённые аккаунты с соглашением NDA.

---

**Q: Как использовать Cursor IDE и MCP для 1С?**

A: Установите Cursor IDE → подключите пакет `1c-ai-development-kit` (правила и агенты под синтаксис BSL) → опционально настройте MCP-сервер `spremotely-vanessa-app-mcp` для автогенерации BDD-тестов. Инструкции — в README каждого проекта на GitHub.

---

### 🏗️ EDT, Конфигуратор и Git

**Q: Когда переходить с Конфигуратора на 1С:EDT?**

A: Рекомендуемая стратегия:
- **Junior (0–6 мес.):** Учитесь в Конфигураторе — он проще для понимания структуры метаданных.
- **Junior+ (6–12 мес.):** Параллельно осваивайте 1С:EDT — базовые операции, синхронизацию с Git.
- **Middle+:** 1С:EDT становится основной средой. Командная разработка без EDT невозможна.

Git — изучайте **с первых месяцев**, независимо от среды.

---

**Q: Как выбрать между 1С:Предприятие и 1С:Элемент для нового проекта?**

A: Простое правило:
- **1С:Предприятие 8.x** → Если нужен Back-office: ERP, бухучёт, ЗУП, управление производством. Строгая транзакционная целостность, сложные блокировки, работа с миллионами записей.
- **1С:Элемент** → Если нужен Front-office: личные кабинеты для клиентов, B2C-маркетплейсы, корпоративные порталы. Нужен красивый кастомный UI (HTML/CSS/JS), масштабирование для тысяч внешних пользователей.
- **Гибрид (чаще всего):** 1С:ERP — ядро данных, 1С:Элемент — веб-интерфейс, связка через REST API / 1С:Шина.

---

### 💼 Карьера и трудоустройство

**Q: Когда можно начинать искать работу?**

A: После 3–4 месяцев обучения (по завершении блока BSL + запросы + основы метаданных). Целевые позиции: Стажёр 1С, Junior (помощник), специалист технической поддержки 1С. **Не ждите «идеальной» готовности** — реальный опыт на работе ускоряет рост в разы.

---

**Q: Какие конфигурации изучать в первую очередь?**

A:
1. **«1С:Бухгалтерия предприятия» (БП)** — абсолютная база. Максимальный спрос на рынке.
2. **«1С:Управление торговлей» (УТ)** — торговые процессы, маркировка, склад.
3. **«1С:ERP Управление предприятием»** — для Middle+, если цель — крупные Enterprise-проекты.
4. **«1С:Зарплата и Управление Персоналом» (ЗУП)** — если интересует HR-направление.
5. **«1С:Элемент»** — если хотите в B2C-сегмент и фронтенд-разработку (отдельный трек).

> Не пытайтесь охватить всё сразу. БП → УТ — правильный старт. ERP — после 1–2 лет практики.

---

**Q: Нужен ли английский язык?**

A: В Конфигураторе и BSL — нет. Но для работы с PostgreSQL, настройки CI/CD, чтения документации 1С:EDT на GitHub, понимания MCP-серверов и веб-архитектуры 1С:Элемента — **базовый технический английский критически необходим**. Уровень Intermediate достаточен.

---

**Q: Сколько часов в неделю нужно учиться?**

A: **20–25 часов/нед** — комфортный темп при совмещении с работой (план рассчитан на 12 месяцев). При **full-time обучении** (40 ч/нед) — сроки сокращаются вдвое. Меньше 10 ч/нед — прогресс будет, но медленным.

---

## 💡 Советы экспертов (Best Practices)

### 1. Строгая архитектурная гигиена (главное правило 2026)

> «В 2026 году «грязный код» — изменение типовых модулей напрямую — это угроза бизнесу. Накатка патча с новыми форматами ЭДО и расчётом НДС 22% должна занимать минуты, а не дни ручного слияния. Используйте только Расширения!»
> — Тимлид команды внедрения 1С:ERP

**Правило:** Никогда не снимайте конфигурацию с поддержки полностью без 100% обоснования. Вся логика доработок — в **Расширениях конфигурации** или через механизмы переопределения **БСП**.

### 2. Тестируйте поведение, а не кнопки (BDD)

Забудьте про ручное «прокликивание» базы после каждого обновления. Пишите сценарии на TurboGherkin для Vanessa Automation. Бизнес-аналитик описывает сценарий — вы автоматизируете шаги. Тесты хранятся в Git вместе с кодом.

### 3. Git — с первого дня

Изучите базовые команды параллельно с основами BSL. Вы должны уметь: создать ветку, откатить изменения, посмотреть историю коммитов, разрешить конфликты слияния.

### 4. Понимайте бизнес, не только код

Спросите у бухгалтера: «Как проводится возврат товара от покупателя с НДС?» Разработчик, понимающий учёт, стоит в 2–3 раза дороже того, кто только пишет код по ТЗ.

### 5. AI как второй пилот, а не водитель

Используйте Cursor IDE для рефакторинга и кодогенерации, но **всегда проверяйте результат**. AI не знает конкретных бизнес-правил вашего заказчика и может сгенерировать синтаксически верный, но логически неверный код.

---

## ⚠️ Подводные камни и частые ошибки

### Уровень: Базовый

**1. Запросы в цикле** 🔴 Критично!

```bsl
// ❌ ПЛОХО: запрос выполняется N раз (N = количество строк в таблице)
Для Каждого Строка Из ТаблицаДокументов Цикл
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ Остаток ИЗ РегистрНакопления.ТоварыНаСкладах.Остатки
                   |ГДЕ Номенклатура = &Номенклатура";
    Запрос.УстановитьПараметр("Номенклатура", Строка.Номенклатура);
    Результат = Запрос.Выполнить();
КонецЦикла;
```

```bsl
// ✅ ХОРОШО: один запрос для всех строк с условием В (&Список)
СписокНоменклатуры = ТаблицаДокументов.ВыгрузитьКолонку("Номенклатура");

Запрос = Новый Запрос;
Запрос.Текст = "ВЫБРАТЬ
               |    Номенклатура,
               |    КоличествоОстаток
               |ИЗ РегистрНакопления.ТоварыНаСкладах.Остатки
               |ГДЕ Номенклатура В (&СписокНоменклатуры)";
Запрос.УстановитьПараметр("СписокНоменклатуры", СписокНоменклатуры);
Результат = Запрос.Выполнить().Выгрузить();
```

---

**2. Обращение к базе данных на клиенте** 🔴

```bsl
// ❌ ПЛОХО: &НаКлиенте не имеет доступа к серверным объектам
&НаКлиенте
Процедура НажатиеКнопки(Команда)
    // Эта строка вызовет ошибку - объект недоступен на клиенте
    СправочникСсылка = Справочники.Номенклатура.НайтиПоНаименованию("Товар");
КонецПроцедуры

// ✅ ХОРОШО: разделяем клиентский вызов и серверную логику
&НаКлиенте
Процедура НажатиеКнопки(Команда)
    НайтиНоменклатуруНаСервере("Товар");
КонецПроцедуры

&НаСервере
Процедура НайтиНоменклатуруНаСервере(Наименование)
    СправочникСсылка = Справочники.Номенклатура.НайтиПоНаименованию(Наименование);
    // ... дальнейшая логика
КонецПроцедуры
```

---

**3. Модальные окна блокируют веб-клиент** 🟡

```bsl
// ❌ ПЛОХО: блокирует вкладку браузера в веб-клиенте (платформа 8.5)
Предупреждение("Документ записан!");
Ответ = Вопрос("Провести документ?", РежимДиалогаВопрос.ДаНет);

// ✅ ХОРОШО: асинхронные вызовы (немодальный режим платформы 8.5)
ПоказатьПредупреждение(, "Документ записан!");

ОповещениеОЗавершении = Новый ОписаниеОповещения("ОбработатьОтветПроведения", ЭтотОбъект);
ПоказатьВопрос(ОповещениеОЗавершении, "Провести документ?", РежимДиалогаВопрос.ДаНет);
```

---

### Уровень: Продвинутый

**4. Прямое изменение типовых модулей** 🔴 Критично для 2026!

**Проблема:** При обновлении базы (например, патч с расчётом НДС 22% или новыми форматами ЭДО/МЧД) система предлагает «слить» конфигурацию вручную. Если типовых модулей затронуто много — это дни ручной работы с высоким риском ошибок.

**Решение:** **Расширения конфигурации.** Весь доработанный код живёт в расширении — ядро типовой конфигурации обновляется автоматически за минуты.

```bsl
// В расширении — перехватываем обработчик типового модуля:
// Аннотация &Вместо заменяет исходную процедуру
&Вместо("ОбработкаПроведения")
Процедура Расш_ОбработкаПроведения(Отказ, РежимПроведения)
    // Наша специфическая логика ДО типовой
    ВыполнитьПроверкуДополнительнаяЛогика(Отказ);
    
    // Вызываем исходную типовую процедуру
    ПродолжитьВызов(Отказ, РежимПроведения);
КонецПроцедуры
```

---

**5. Переход на Linux: case-sensitivity** 🟡

**Проблема:** Windows прощает ошибки регистра (`Report.xml` и `report.xml` — одно и то же). Linux — **регистрозависимый**. При миграции на Astra Linux / РЕД ОС ломаются: пути к файлам в выгрузках, HTTP-интеграции, внешние обработки.

**Решение:** Пишите все пути к файлам и URL строчными буквами. Проводите тесты интеграций на Linux-стенде до перехода в продуктив.

---

**6. Idle Timeout и фоновые операции** 🟡

**Проблема:** Разработчик запускает длинную обработку перепроведения в интерактивном клиентском сеансе. Срабатывает таймаут бездействия (Idle Timeout, новый в платформе 8.5) — сервер принудительно обрывает сеанс, обработка прерывается на полпути.

**Решение:** Всегда выполняйте тяжёлые длительные операции через **Фоновые задания** (`ФоновыеЗадания.Выполнить()`). Пользовательский интерфейс получает уведомление о завершении через механизм «Уведомления клиента» (Client Notifications API, платформа 8.5).

---

**7. Новый алгоритм хеширования PBKDF2 — необратимое решение** 🟡

**Проблема:** Администратор переводит базу на алгоритм PBKDF2-HMAC-SHA256. Это делает **даунгрейд платформы невозможным** — база уже не запустится на старой версии.

**Решение:** Всегда делайте резервную копию перед тестированием новых релизов 8.5. Смену алгоритма хеширования применяйте только после тщательного тестирования на копии.

---

**8. Гибридные архитектуры (ERP + Элемент)** 🟡

**Проблема:** Попытка написать тяжёлый транзакционный код с блокировками в 1С:Элемент.

**Решение:** Помните архитектурное разделение: 1С:Элемент — **асинхронный микросервисный фронтенд**. Вся тяжёлая транзакционная логика (расчёт себестоимости, проведение документов) должна оставаться в 1С:Предприятии. Элемент обращается к ней по REST API — только для чтения данных или фиксации транзакции в нужный момент.

---

[← Ресурсы](05_Resources.md) | [🗺️ На главную](README.md)